<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.edu.uns.cs.trabajosocialform">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:windowSoftInputMode="stateAlwaysHidden">
        <activity android:name=".Presentation.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Presentation.activities.FormSolicitanteActivity"/>
        <activity android:name=".Presentation.activities.FormApoderadoAcitivity" />
        <activity android:name=".Presentation.activities.FormDomicilioActivity" />
        <activity android:name=".Presentation.activities.FormSituacionHabitacionalActivity" />
        <activity android:name=".Presentation.activities.FormCaracteristicasViviendaActivity" />
        <activity android:name=".Presentation.activities.FormInfraestructuraBarrialActivity" />
        <activity android:name=".Presentation.activities.FormGrupoFamiliarActivity" />
        <activity android:name=".Presentation.mvp.view.NuevoFamiliarView" />
        <activity android:name=".Presentation.activities.FormularioActivity" />
        <activity android:name=".Presentation.activities.EntradasActivity" />
        <activity android:name=".Presentation.activities.DetallesFormActivity" />
        <activity android:name=".Presentation.activities.DetallesFamiliarActivity" />

    </application>

</manifest>